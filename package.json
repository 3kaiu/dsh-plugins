{
  "name": "dsh-plugins-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo of DeepSeek Harness plugins: OpenCode Zen LLM adapter, layout inference, UI restore core and visual reverse agent.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test && node scripts/check-fork-parity.mjs",
    "install:local": "pnpm build && node scripts/install-local.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "license": "MIT"
}
