{
  "name": "dsh-plugins-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo of DeepSeek Harness plugins: OpenCode Zen LLM adapter, harness updater, layout inference.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "install:local": "pnpm build && node scripts/install-local.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "license": "MIT"
}
