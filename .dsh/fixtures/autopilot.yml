# fixture:autopilot 契约样例(loadContract 解析字段对齐;列表必须行内形态,yaml-mini 限制)
budget:
  max_runs_per_day: 3
  max_attempts_per_issue: 3
  max_changed_files: 15
  max_diff_lines: 500
  max_runtime: 15m
permissions:
  allow: ["packages/**", "docs/**", "tests/**"]
  deny: [".github/workflows/*", "release.yml"]
