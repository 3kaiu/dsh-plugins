// @3kaiu/dsh-plugins-ui —— 设置弹窗「插件」tab 的浏览器侧卡片(node half 占位)
// 空 apply:让插件出现在 host cordis.yml / Loader;全部行为在 browser half
// (dist/client.js,经 package.json 的 dsh.client 声明被 host 自动发现注入)。
function apply() {}
export { apply };
